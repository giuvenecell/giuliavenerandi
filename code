/* styles.css */

body {
    font-family: Arial, sans-serif;
    background: linear-gradient(to bottom, #d3eaff, #f5ede0);
    margin: 0;
    display: flex;
    flex-direction: column;
    height: 100vh;
}

header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
}

header h1 {
    margin: 0;
}

nav ul {
    list-style: none;
    display: flex;
    padding: 0;
    margin: 0;
}

nav li {
    margin: 0 15px;
}

nav a {
    text-decoration: none;
    color: black;
}

nav a.active {
    text-decoration: underline;
}

.menu {
    cursor: pointer;
}

main {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
}

.sphere-container {
    position: relative;
    text-align: center;
}

.label {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    display: flex;
    align-items: center;
}

.label .close {
    cursor: pointer;
    margin-left: 5px;
}

footer {
    display: flex;
    justify-content: space-between;
    padding: 20px;
}
